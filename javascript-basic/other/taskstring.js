// Task 1 কতগুলা ‍a আছে
/* const banana = 'stringa';
let sum = '';
for (const b of banana) {
    if (b === 'a') {
        sum = sum + b
    }
}
const totalA = sum.length
if (totalA > 0) {
    // console.log(totalA)
}
else {
    // console.log('Not found')
} */



//Task 2 Check whether a string contains all the vowels a, e, i, o, u(একটি string এ সমস্ত স্বরবর্ন আছে কিনা)
/* const stringVowel = 'for ever you re my lke';
if (stringVowel.includes('a') && stringVowel.includes('e') && stringVowel.includes('i') && stringVowel.includes('o') && stringVowel.includes('u')) {
    console.log('In the string all vowel are found')
}
else if (stringVowel.includes('a')) {
    console.log('Only a Vowel found')
}
else {
    console.log('Nothing vowel found')
} */

// Task 3 Replace a character in a string

/* const fruit = 'Apdle'
const fruitReplace = fruit.replace('d', 'p')
console.log(fruit, fruitReplace) */

// Task 4 Capitalize Every first Letter of each word in a String
/**
 * পরে বুঝে নেয়ার ব্যবস্থা করে নিতে হবে।
 */