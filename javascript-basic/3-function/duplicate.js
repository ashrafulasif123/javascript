/**
 * numbers duplicate to unique
 */

/* function duplicates(numbers) {
    const uniqueNumber = []
    for (const number of numbers) {
        if (uniqueNumber.includes(number) === false) {
            uniqueNumber.push(number)
        }
    }
    return uniqueNumber
}
console.log(duplicates([14, 25, 14, 50, 50, 47, 63, 21])) */

/**
 * String duplicates to Unique
 */

/* function duplicateString(duplicateStr) {
    const uniqueString = []
    const duplicateStrSplit = duplicateStr.split(' ')
    for (const duplicateWord of duplicateStrSplit) {
        if (uniqueString.includes(duplicateWord) === false) {
            uniqueString.push(duplicateWord)
        }
    }
    return uniqueString.join(' ')
}

const duplicateStr = 'what is your your name please tell tell me'
console.log(duplicateStr)
const resultUniquString = duplicateString(duplicateStr)
console.log(resultUniquString) */

